import React,{useState} from "react";
import {Form,Col,Button,Table} from 'react-bootstrap';
import style from './SingleAppointment.module.css'

export default function (props) {
    const [key,setKey] = useState(true)
    return(
        <div>

        </div>
    )
}